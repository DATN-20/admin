version: '3.9'

services:
  admin:
    container_name: adminartists
    image: nightcrawlers512/adminartists:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .docker.env
    ports:
      - "3002:3002"